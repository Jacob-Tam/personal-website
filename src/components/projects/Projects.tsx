// Step 2 stub. Real projects (staggered cards, faint PROJECTS word, scroll indicator,
// clip-path opener, expanded overlay) are built in Steps 6/9/10.
export function Projects() {
  return (
    <section
      id="projects"
      className="flex min-h-svh flex-col items-center justify-center border-t border-border"
    >
      <span className="font-mono text-label uppercase text-text-mute">04 / projects</span>
    </section>
  )
}
