// Step 2 stub. Real hero (name + tagline overlay over the orb) is built in Step 6.
export function Hero() {
  return (
    <section id="hero" className="flex min-h-svh flex-col items-center justify-center">
      <span className="font-mono text-label uppercase text-text-mute">01 / hero</span>
    </section>
  )
}
